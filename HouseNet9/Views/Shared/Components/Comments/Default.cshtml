 @model List<Data.Data.HouseRentalData.Comment>



@*
<div class="container my-5">
    <h3 class="mb-4">Opinie klientów</h3>

    <div class="row g-4" id="commentsContainer">
        @foreach (var comment in Model)
        {
            <div class="col-12 col-md-6 col-lg-4">
                <div class="card shadow-sm h-100 border-0 comment-card">
                    <div class="card-body d-flex flex-column">

                        <h5 class="card-title mb-1">
                            @comment.AuthorName
                        </h5>

                        <div class="mb-2 text-warning">
                            @for (int i = 0; i < comment.Rating; i++)
                            {
                                <span>★</span>
                            }
                        </div>

                        <p class="card-text flex-grow-1">
                            @comment.Text
                        </p>

                        <small class="text-muted">
                            @comment.CreatedAt.ToShortDateString()
                        </small>

                        @if (!string.IsNullOrEmpty(comment.AdminText))
                        {
                            <div class="mt-3 p-2 bg-light rounded">
                                <strong>Odpowiedź właściciela:</strong>
                                <p class="mb-0">@comment.AdminText</p>
                            </div>
                        }

                    </div>
                </div>
            </div>
        }
    </div>

    <div class="text-center mt-4">
        <button id="loadMoreBtn" class="btn btn-outline-dark">
            Pokaż więcej
        </button>
    </div>
</div> *@



<div class="container my-5">
    <h3 class="mb-4">Opinie klientów</h3>

    <div id="commentsContainer">
        @await Html.PartialAsync("_CommentsList", Model)
    </div>

@*     <div class="text-center mt-4">
        <button id="loadMoreBtn"
                class="btn btn-outline-dark"
                data-house="@ViewBag.HouseId"
                data-skip="@Model.Count">
            Pokaż więcej
        </button>
    </div> *@
</div>